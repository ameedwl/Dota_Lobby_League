"use client";import Link from "next/link";import {useMemo,useState} from "react";import {PlayerStats} from "@/lib/types";import {Avatar,FormDots} from "./ui";import {Crown} from "./icons";
type Sort=keyof Pick<PlayerStats,"games"|"wins"|"losses"|"winRate"|"bestWinStreak"|"mvps">;
export function Leaderboard({stats}:{stats:PlayerStats[]}){const [sort,setSort]=useState<Sort>("wins");const rows=useMemo(()=>[...stats].sort((a,b)=>b[sort]-a[sort]||b.wins-a.wins),[stats,sort]);return <div className="table-wrap"><table><thead><tr><th>Rank</th><th>Player</th>{[["games","Games"],["wins","Wins"],["losses","Losses"],["winRate","Win rate"],["bestWinStreak","Best streak"],["mvps","MVPs"]].map(([k,v])=><th key={k} onClick={()=>setSort(k as Sort)}>{v}{sort===k?" ↓":""}</th>)}<th>Current / Form</th></tr></thead><tbody>{rows.map((s,i)=><tr key={s.player.id}><td><span className={`rank ${i===0?"gold":""}`}>{i===0&&<Crown size={19}/>}#{i+1}</span></td><td><Link className="inline-player" href={`/players/${s.player.id}`}><Avatar player={s.player}/><span><b>{s.player.name}</b><small className="nickname" style={{display:"block"}}>{s.player.nickname}</small></span></Link></td><td>{s.games}</td><td>{s.wins}</td><td>{s.losses}</td><td><b>{s.winRate.toFixed(1)}%</b></td><td>{s.bestWinStreak}W</td><td>{s.mvps}</td><td><span className={s.streakType==="W"?"win badge":"loss badge"}>{s.currentStreak}{s.streakType}</span> <FormDots form={s.recentForm}/></td></tr>)}</tbody></table></div>}
